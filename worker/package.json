{
  "name": "traychen-gate",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "hash": "node scripts/hash-password.mjs",
    "upload": "./scripts/upload-assets.sh"
  },
  "devDependencies": {
    "wrangler": "^4.42.0"
  }
}
